var chai = require('chai');
var expect = chai.expect;
var streams = require('../src/streams');

describe('message handler',  function() {
  var OK = 'ok';
  var HANDLED = 'handled';

  var incomingMessageStream = streams.Console;

});
